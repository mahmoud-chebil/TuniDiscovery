{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1> Hello this is simple Symfony application with api Orange sms ✅</h1>
        <div align="center"><p>
                <a href="{{ path('smsapi') }}">SEND SMS</a>
                <a href="{{ path('getadmincontracy') }}">GET ADMIN CONTRACT</a>
                <a href="{{ path('getAdminStats') }}">GET ADMIN STATS</a>
                <a href="{{ path('getAdminPurchased') }}">GET ADMIB PURCHASEDS</a>

            </p></div>
        {{ form_start(form, {'attr': {'class': ''}}) }}
        {{ form_errors(form)}}

        {{ form_end(form) }}

    </div>
{% endblock %}