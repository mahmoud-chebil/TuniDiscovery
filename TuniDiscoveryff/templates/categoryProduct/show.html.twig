
{{ include('header.html.twig') }}
{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('CategoryProduct_index') }}">back to list</a>

    <a href="{{ path('CategoryProduct_edit', {'id': category.id}) }}">edit</a>

    {{ include('CategoryProduct/_delete_form.html.twig') }}
{% endblock %}
{{ include('footer.html.twig') }}