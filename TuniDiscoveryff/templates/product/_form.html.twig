{{ form_start(form,{'attr':{'novalidate':'novalidate'}}) }}
    {{ form_widget(form) }}

    <button class="btn btn-danger"  style="width: 50%">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
